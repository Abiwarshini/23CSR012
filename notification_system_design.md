Stage 2:

The stage 2 contain the query thing of no sql database mongodb:
            admin.database.findOne({Message="farewell"});
            admin.dtabase.find({Message="placement"});


Stage 3:

the team suggest to add the index is a good idea, 
we are going to do the notification as a index it will fetch the data quickly and return the retrived value easily 


Stage 4:

   Instead of fetching all the things in the single page we can use the pagination concept to overcome the thing of db and UI onverload by limit the nofification upto 10 and skip the remaining to another page will help.


Stage 5:
             
